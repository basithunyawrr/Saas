# EduFlow

Modern school management SaaS.

## Development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
npm run preview
```

Netlify is configured through `netlify.toml` to build the Vite app and publish `dist/`, with SPA fallback routing.
